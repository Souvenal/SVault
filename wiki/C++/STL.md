# std::addressof

获取对象地址的运算符，但能避免被重载的 `&` 运算符误导。

```c++
T* ptr = std::addressof(obj);
```

## 为什么不用 `&`

自定义类型可能重载了 `&` 运算符，返回的不是真实内存地址。`std::addressof` 使用编译器内置函数保证返回对象真实地址。

## 右值参数

传入右值会触发编译错误：

```c++
std::addressof(T()); // 编译错误：右值没有地址
```
