# Neovim 插件

Neovim 本身没有内置插件管理，需要先安装插件管理器（如 lazy.nvim）。

---

## 常用插件

### which-key

按键提示插件，显示可用快捷键。

### telescope

模糊查找器，用于文件搜索、grep 等。

### leap.nvim

快速跳转插件。

```
按 s → 输入搜索字符 → 高亮匹配 → 显示字母标签 → 输入标签跳转
```

### mini.animate

光标跳转动画效果。

---

## See Also

- [[Lua-Architecture]] — 插件管理器原理
