# 参数化曲线

实数区间到向量值函数的映射。

---

## 基本概念

**参数化曲线定义**：一段实数区间到一个向量值函数的映射。

**曲线等价**：存在对于实数区间的连续单调满射。

---

## 曲线分类

| 类型 | 条件 |
|------|------|
| 连续曲线 | 映射连续 |
| $C^k$ 曲线 | 映射 $k$ 阶可导且导数连续 |
| 光滑曲线 | 映射无限可导 |
| 正则曲线 | 导数处处非零 |

> 正则曲线为什么没有尖点？
> 正则曲线的导数处处非零，因此切向量连续变化，不会出现方向突变导致的尖点。
